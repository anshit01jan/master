# Ignore log files - they may contain sensitive information
# Generated audit artifacts are intentionally excluded from PR diffs.
# PR scope refresh note: comment-only touch to keep this ignore file visible in PR #4.
audit.log
violations.log
report_*.txt

# Ignore temporary files
*.tmp
temp_*

# Keep structure but ignore content
!.gitkeep
